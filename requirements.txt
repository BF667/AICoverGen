# AICoverGen dependencies
#
# The RVC inference stack is now provided by the `uziproj/rvc`
# package (https://github.com/uziproj/rvc). It pulls in its own
# recent versions of torch, torchaudio, faiss-cpu, librosa,
# praat-parselmouth, soundfile, numpy, scipy, transformers, etc.
# We therefore no longer pin the older versions of those packages
# that AICoverGen used to ship. Pins that remain below are for
# AICoverGen-specific tools (gradio, pedalboard, pydub, sox) that
# are NOT part of the RVC inference path.

# --- RVC inference (modern uziproj/rvc package) ----------------------
# Installs the rvc package from GitHub. The package brings its own
# recent torch / torchaudio / faiss / librosa / praat-parselmouth /
# soundfile / numpy / scipy / transformers dependencies — see
# https://github.com/uziproj/rvc/blob/main/pyproject.toml for the
# full list.
rvc @ git+https://github.com/uziproj/rvc.git

# --- AICoverGen-specific tooling (not RVC-related) -------------------
deemix
ffmpeg-python>=0.2.0
gradio==5.44.0
lib==4.0.0
pedalboard==0.7.7
pydub==0.25.1
Requests>=2.31.0
sox==1.4.1
noisereduce
spaces
matplotlib-inline
yt_dlp
tqdm>=4.65.0
